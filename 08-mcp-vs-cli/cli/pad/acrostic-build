#!/usr/bin/env python3
"""Build an acrostic whose initials spell a message."""
import argparse, sys

p = argparse.ArgumentParser(prog="acrostic-build", description="Build an acrostic whose initials spell a message.")
p.add_argument("--message", type=str, required=True, help="message")
p.add_argument("--theme", type=str, default="any", help="theme")
a = p.parse_args()
print("acrostic-build: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
