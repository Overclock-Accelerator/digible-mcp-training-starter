#!/usr/bin/env python3
"""Check whether a string is in the dictionary."""
import argparse, sys

p = argparse.ArgumentParser(prog="validate-word", description="Check whether a string is in the dictionary.")
p.add_argument("--word", type=str, required=True, help="word")
p.add_argument("--dictionary", type=str, default="enable1", help="dictionary")
a = p.parse_args()
print("validate-word: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
