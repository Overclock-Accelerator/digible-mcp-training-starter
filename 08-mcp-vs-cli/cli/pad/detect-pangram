#!/usr/bin/env python3
"""Report whether a passage uses every letter of an alphabet."""
import argparse, sys

p = argparse.ArgumentParser(prog="detect-pangram", description="Report whether a passage uses every letter of an alphabet.")
p.add_argument("--text", type=str, required=True, help="text")
p.add_argument("--alphabet", type=str, default="abcdefghijklmnopqrstuvwxyz", help="alphabet")
a = p.parse_args()
print("detect-pangram: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
