#!/usr/bin/env python3
"""List dictionary words starting with a prefix."""
import argparse, sys

p = argparse.ArgumentParser(prog="prefix-search", description="List dictionary words starting with a prefix.")
p.add_argument("--prefix", type=str, required=True, help="prefix")
p.add_argument("--max-results", type=int, default=25, help="max_results")
a = p.parse_args()
print("prefix-search: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
