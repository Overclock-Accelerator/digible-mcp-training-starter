#!/usr/bin/env python3
"""Return a dictionary definition."""
import argparse, sys

p = argparse.ArgumentParser(prog="define-word", description="Return a dictionary definition.")
p.add_argument("--word", type=str, required=True, help="word")
p.add_argument("--sense", type=int, default=1, help="sense")
a = p.parse_args()
print("define-word: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
