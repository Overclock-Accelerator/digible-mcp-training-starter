#!/usr/bin/env python3
"""Check a passage for misspellings and suggest corrections."""
import argparse, sys

p = argparse.ArgumentParser(prog="check-spelling", description="Check a passage for misspellings and suggest corrections.")
p.add_argument("--text", type=str, required=True, help="text")
p.add_argument("--max-suggestions", type=int, default=5, help="max_suggestions")
a = p.parse_args()
print("check-spelling: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
