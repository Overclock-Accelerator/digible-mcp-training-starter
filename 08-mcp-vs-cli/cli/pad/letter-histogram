#!/usr/bin/env python3
"""Count letter frequencies in a passage."""
import argparse, sys

p = argparse.ArgumentParser(prog="letter-histogram", description="Count letter frequencies in a passage.")
p.add_argument("--text", type=str, required=True, help="text")
p.add_argument("--normalize", type=str, default=False, help="normalize")
a = p.parse_args()
print("letter-histogram: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
