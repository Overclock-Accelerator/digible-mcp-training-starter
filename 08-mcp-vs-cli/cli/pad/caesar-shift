#!/usr/bin/env python3
"""Apply a Caesar shift to a passage."""
import argparse, sys

p = argparse.ArgumentParser(prog="caesar-shift", description="Apply a Caesar shift to a passage.")
p.add_argument("--text", type=str, required=True, help="text")
p.add_argument("--shift", type=int, default=3, help="shift")
a = p.parse_args()
print("caesar-shift: not implemented — this is a benchmark pad command.", file=sys.stderr)
raise SystemExit(3)
